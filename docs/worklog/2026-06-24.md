# 작업일지 - 2026-06-24 (기간 2026-06-22~24)

## 작업 내역

### [기안씨] 노션 문서 → 공문서 스타일 PDF 자동변환 구현
- 레포 클론 후 D:(exFAT, Next16 심링크 불가) → C:(NTFS) 이전
- 노션 MD/SDK → DocumentIR → puppeteer HTML→PDF 파이프라인 구축
  - 표/중첩 리스트/컬럼(가로 미러링)/이미지 보존
  - 공문서 양식: 목차(실측 페이지번호), 단원별 새 페이지, 컬러 섹션 헤더, □○ 불릿, 회색 표 헤더
  - 이미지: 다운로드 → sharp 1100px 리사이즈 → base64 인라인
  - 목차 페이지번호: unpdf로 PDF 파싱해 자체 실측 (2-pass)
- **런타임 자동변환**: 노션 PAT 토큰 → `POST /api/convert {notionUrl}` → PDF
  - `client.ts`(URL파싱·계층보존 트리·속성), `blocks-to-ir.ts`(SDK 블록→IR)
- **결과물**: `src/lib/*`, `app/api/convert`, 플레이북 `CLAUDE.md`, 커밋 `e5988b6` 푸시

## 이슈
- D: exFAT 심링크 불가 → C: 이전으로 해결
- PowerShell ConvertTo-Json 대용량 문자열/한글 깨짐 → Node fetch 사용
- PDF 확인용 뷰어 부재 → PyMuPDF로 PNG 렌더
- 노션 이미지 S3 URL 1시간 만료 + 원본 대용량 → 즉시 다운로드 + sharp 리사이즈
- 검증은 추측·눈대중 금지, PDF 텍스트 추출로 데이터 누락 확인 (fixture 임의 축소 금지)

## 다음 할 일
- 트리거(노션 버튼/Teams 봇)로 변환 호출 연동
- 양식 템플릿 추가(기안서/회의록), CLI 단독 실행
